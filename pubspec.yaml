name: hybrid_gift
description: Application for hybrid gifting
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cloud_firestore: ^3.1.0
  firebase_auth: ^3.2.0
  firebase_core: ^1.10.0
  tflite_flutter: ^0.9.0
  textfield_search: ^0.9.2
  flutter:
    sdk: flutter
  google_fonts: ^2.0.0
  provider: ^6.0.1
  cupertino_icons: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/
